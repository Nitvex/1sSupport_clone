import { Tab } from "@material-ui/core";
import { withStyles } from "@material-ui/core/styles";

const OCSTab = withStyles(() => ({
  wrapper: {
    fontSize: "1.2rem"
  }
}))(Tab);

export default OCSTab;
