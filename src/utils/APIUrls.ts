export const serverDomain: string = "https://api.1ssupport.ru/v0.1";

const identityService: string = "identity";

export const APIUrls: object = Object.freeze({
  SIGN_IN: `${serverDomain}/${identityService}/sign-in`
});
